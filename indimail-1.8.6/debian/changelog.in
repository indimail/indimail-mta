indimail (@version@-1) unstable; urgency=low

  * autoresponder.c - Added description for -T option
  * dkim.c - do not skip X-Mailer headers
  * qmail-dkim.c - do not skip X-Mailer headers
  * BUG - tcpserver.c - wrong variable used for ip address causing fnrules to fail in ip address matching
  * made diskquota, maxmsgcount, defaultquota, defaultmaxmsgcount in vlimits unsigned
  * indisrvr.c - removed pidfile
  * dns.c - added DISABLE_CNAME_LOOKUP for qmail-remote
  * new program rrt for handling Return Receipts
  * qmail-remote - set ONSUCCESS_REMOTE, ONFAILURE_REMOTE, ONTRANSIENT_REMOTE selectively in run_script
  * qmail-remote - fix SIGSEGV in run_script
  * qmail-remote - set [S|Q]MTPTEXT, [S|Q]MTPCODE for transient errors
  * qmail-remote - fix smtptext during TLS
  * FORCE_TLS functionality in qmail-smtpd
  * Upgraded to fetchmail 6.3.24

 -- Manvendra Bhangui <manvendra@indimail.org>  Thu, 22 Jan 2013 18:38:40 +0530
