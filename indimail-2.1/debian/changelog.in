indimail (@version@-3) unstable; urgency=low

  * quoted svctool arguments and save install time arguments in variables/.options file
  * fixed lang and html directory location to /usr/share/iwebadmin
  * iwebadmin (user.c) - vsetuserquota returns current quota usage and -1 in case of error.
  * iwebadmin should treat only -1 as error.
  * Added roundcube plugins, (sauserprefs, markasjunk2) - bogofilter spam training, (iwebadmin) - for setting autoresponder and passwd change
  * update_tmprsadh, svctool, spec files, debian postinst, postrm - updated certificate location to sysconfdir/certs
  * qmail-smtpd, qmail-remote - Env variable CERTDIR to override /etc/indimail/control for certificate location
  * Permissions of supervise directory changed to onwer root:indimail 750 for services using MAXDAEMONS
  * renamed qmail-spamfifo service to qmail-logfifo
  * Changed inquery path. Added infifo argument for smtp/pop3/imap creation in
  * Added FIFODIR env variables for infifo location (inlookup, smtp/pop3/imap services) svctool, spec file, debian postinst, create_services
  * Added back AutoReqProv in spec file to provide shared libraries
  * Upgraded to courier-imap-4.17.3
  * added upgrade script upgrade.sh, local_upgrade.sh
  * update_tmprsadh - added rsa2048.pem, dh2048.pem
  * ucspi-tcp - control_readline() did not use CONTROLDIR. Caused problem with qmail_smtpd.so plugin which used the same symbol and was unable to load control files.
  * fixed conf_dir variable getting clobbered by qmail-logfifo service creation.
  * create tlsservermethod control file in svctool --config=qmail
  * Increased compile time default quota for users to 500 MiB from earlier value of 5 MiB
  * renamed functions in ucspi-tcp (rblsmtpd.so) to avoid duplicate symbols with qmail_smtpd.so
  * use RTLD_DEEPBIND (if present) in load_shared.c to avoid duplicate symbols when loading multiple shared libs
  * fixed paths in crontab file
  * set DEFAULT_DOMAIN in qmail-send.25/variables as the hostname to handle double bounces better
  * qmail-todo, qmail-send - display null email address as <> in log_stat()
  * svctool --config=qmail - set envnoathost, defaulthost = hostname 
  * local_upgrade.sh - fix DEFAULT_HOST, QMAILDEFAULTHOST, envnoathost, defaulthost settings
  * svctool - MRTG graphs, added --mrtg option
  * indimail.mrtg.cfg - New graphs added
  * qmailmrtg7.c - added debug option
  * indimai.spec - added mrtg in Requires
  * mrtg service moved to /service/mrtg from cronlist
  * added error logging for iwebadmin
  * local_upgrade.sh added FIFODIR env variable
  * iwebadmin.h - Increaed MAX_LANG_STR length
  * util.c - undefine SORT_TABLE_ENTRIES
  * iwebadmin - Added Content-type, Content-transfer-encoding headers to lang/en
  * iwebadmin - Reserved language template file codes 400 through 450 for custom autoresponder headers
  * iwebadmin - Added url for indimail MRTG graphs
  * iwebadmin - templace.c - skip custom headers
  * iwebadmin - maillinglist.c - changes for ezmlm-idx-7.x
  * iwebadmin - mailinglist.c - fixed syntax error when compiling on host with ezmlm-idx
  * indimail.te - added rename cabability for iwebadmin on var_t
  * replace dlopen() with dlmopen to keep tcpserver namespace private
  * added dlnamespace for using dlmopen()
  * tcpserver.c, prot.c - Changed data types for uid, gid to uid_t, gid_t
  * Bug with dlmopen(), glibc wrappers for setgroups, setgid, setuid - use syscall for setgroups, setgid, setuid
  * tcpserver - execute tcpserver_plugin after shedding root privileges
  * indimail.postinst.in, indimail.spec, local_upgrade.sh,
  * iwebadmin.spec - added ezmlm-idx > 7.2 in Requires: indimail-mta.postinst.in, indimail-mta.spec - upgraded softmem to 536870912
  * svctool - Fixed couriersslcache path
  * dlnamespace.c - added comments
  * qmail-remote.c - fixed tls_quit() function. Avoid mixed usage of tls_quit() and quit() functions
  * debian - replaced tcl8.4-dev with tcl-dev in indium dsc file
  * debian - replaced mutiple dsc files with single dsc file
  * debian - use dh_install in rules to fix broken build bogofilter-wordlist, indium, iwebadmin
  * qmail-remote.c, rrt.c - renamed ONTRANSIENT_REMOTE to ONTEMPORARY_REMOTE
  * qmail-rspawn.c Unset QMAILLOCAL in parent & not in child after vfork
  * perms of /etc/indimail and subdirectory changed to 2775
  * qmail-remote.c - use SMTPS if env variable SMTPS is set
  * qmail-queue.c - fixed envheader. variable x renamed to field_len and initialized to prevent skipping.
  * tinydnssec-1.05 package added. Default installation provides caching dns
  * added selinux rules for tinydnssec
  * fixed sysconfdir and sysconfdir subdir permissions
  * use #ifdef HASDLMOPEN to use dlmopen() if present
  * tcpserver_plugin.c - use PLUGINn_DIR variable to change directory independent of PLUGINn_init env variable
  * create queue base directory in setup
  * report programs moved to libexecdir
  * surblqueue - fixed qmail-multi path
  * fixed search using hsearch in qmail-greyd.c
  * tcpserver - load_shared.c - print lmid only during reload
  * moved qmail-greyd, greydaemon, surblfilter, surbqueue to /usr/sbin
  * use lockfile .svlock in service directory to prevent multiple svscan to run on a service directory
  * Major cleanup of indimail.spec, indimail-mta.spec. Removed deletion of files and leave it for rpm -e to do its job
  * dlnamespace.c, tcpserver_plugin.c, load_shared.c, pathexec_env.c added code to get namespace of shared libraries using env variables / envp variable.  use pathexec_dl to execute loaded function 
  * qmailctl.sh - added multilog rotate command
  * indimail-mta - install crontab entries for indimail-mta
  * indimail debian - install crontab entries for indimail
  * qmail-smtpd - Updated documentation on LOGFILTER
  * qmail-smtpd - renamed LOGFILTER_FD to LOGFD
  * added dqcache, dnscurve
  * indimail, indimail-mta - shutdown indimail service in preun
  * tinydnssec - fixed typo in debian/postrm.in
  * tinydnssec (rpm, debian) - create tinydnssec, dqcache, curvedns service config
  * tinydnssec - added dqcache-conf, curvedns-conf programs
  * tinydnssec - added SRV patch
  * indimail, indimail-mta (rpm, debian) - use svok to figure out if service is up
  * indimail - debian scripts cleanup
  * svctool - fixed inverted cname-lookup
  * indimail, indimail-mta (rpm, debian) - removed duplicate calls to shutdown indimail service
  * tinydnssec - added compile time option to add dnssec, dnscurve support
  * indimail, indimail-mta (rpm, debian) - use sed to edit inittab
  * indimail Makefile.am - removed useless linking if libindimail with other libs
  * indimail-mta - added svctool man page
  * indimail-mta - fixed sysconfdir variable in debian scripts
  * OSx indimail.plist - fixed svscanboot path
  * indimail.h - changed timestamp columns default to CURRENT_TIMESTAMP
  * deldomain.c - fixed incorrect handling of return status from del_control()
  * findhost.c - fixed recursive loop in findhost.c if hostcntrl was empty
  * vdeldomain.c - fixed deletion of mcdinfo
  * vauth_deldomain.c - remove alias entries added by vadddomain for a clustered domain
  * made --enable-large-site=y and --enable-user-cluster=y incompatible
  * removed mailing list code from vfilter
  * fixed DESTDIR in qmail-1.03, ucspi-tcp-1.03 Makefile, setup.c
  * fixed tlsclientmethod path in qmail-remote
  * cleanq.c - refactored code. added checks for safety
  * setup.c, instcheck.c - added logdir
  * svctool - added --skip-sendmail option to disable checking sendmail path in /usr/lib, /usr/sbin
  * Added default.configure in all source directories for source compilation using git repo
  * create_services - added --nomysql  option to skip configuring of mysql services

 -- Manvendra Bhangui <manvendra@indimail.org>  Fri, 05 May 2017 20:42:28 +0530
