indimail-mta (2.4-6) unstable; urgency=low

  * svctool - added option to refresh services
  * moved cdbget, cdbgetm,cdbmake, qmail-getpw, qmail-local, qmail-newmrh to sbin
  * moved system binaries to sbin
  * added permissions files for openSUSE
  * svctool - use SHA-512 encryption for indisrvr admin password
  * svctool - moved enabling/disabling of services to separate functions --enable-service, --disable-service
  * create ld.so cache only when libdir is not /usr/lib or /usr/lib64
  * svctool - added --ssl, --require-secure-transport command line option for mysqld
  * svctool - create env variable VIRTUAL_PKG_LIB with libindimail.so to provide indimail functionality
  * BUG - indimail_stub.c:  fixed spurious chown() in r_mkdir()
  * svctool - fix creation of env variable VIRTUAL_PKG_LIB
  * svctool - create SSL/TLS config for MariaDB like mysql_ssl_rsa_setup utility for mysql-community-server
  * svctool - autofix QHPSI during clamd service creation for smtpd, qmqpd, qmtpd
  * svctool - skip service refresh if .norefreshsvc file is present in variables directory
  * svctool - added --config=sqlssl for creating SSL/TLS config for mariadb
  * str_cpyb.c - fixed extra bytes getting copied
  * qmail-dane - DANE implementation for IndiMail/indimail-mta
  * daneq - Tool to query qmail-dane for TLSA RRs
  * tlsacheck() function to send queries to qmail-dane.
  * qmail-multi.c - fixed qscanq path to sbin
  * fixed do_scan() for badattachment check

 -- Manvendra Bhangui <manvendra@indimail.org>  Sat, 05 May 2018 18:25:22 +0530
