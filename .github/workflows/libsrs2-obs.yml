name: libsrs2 obs trigger

on:
  push:
    paths:
      - '**/libsrs2-x/**'
      - '**/prepare_obs'
      - '**/catChangeLog'
      - .github/workflows/libsrs2-obs.yml
      - '!**/doc/*'
      - '!**.md'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: checkout_libsrs2
      uses: actions/checkout@v2

    - name: prepare obs
      run: sudo apt-get update; sudo apt-get install curl; cd libsrs2-x; ./prepare_obs libsrs2
    - uses: actions/upload-artifact@v2
      with:
        name: libsrs2
        path: ~/stage
    - shell: bash
      name: trigger_obs
      run: |
        mkdir ~/.ssh
        echo "${{secrets.sf_private_key}}" > ~/.private_key
        chmod 600 ~/.private_key
        echo "${{secrets.sf_known_hosts}}" > ~/.ssh/known_hosts
        cd ~/stage
        tar cvfz $GITHUB_WORKSPACE/libsrs2-obs.tar.gz *
        # copy to /home/frs/project/indimail
        scp -q -i ~/.private_key $GITHUB_WORKSPACE/libsrs2-obs.tar.gz  "${{secrets.sf_user}}:${{secrets.sf_upload_path}}"
        rm -f ~/.private_key ~/.ssh/known_hosts $GITHUB_WORKSPACE/libsrs2-obs.tar.gz
        rm -rf ~/stage
