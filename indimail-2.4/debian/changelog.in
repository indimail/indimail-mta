indimail (@version@-1) unstable; urgency=low

  * libdkim - dns.cpp. fixed double free of dnresult causing SIGSEGV
  * skip envdir if .svscan/variables directory is missing
  * libdkim - added CXXFLAGS -Wno-delete-non-virtual-dtor -Wno-reorder to configure.ac
  * libdkim - removed compiler warnings
  * qmail-dkim - added missing DKIM_MFREE
  * indimail-mta, added svctool, systemctl unit file for daemontools package
  * fixed svctool for daemontools add-boot command
  * qmail-cdb.c - fixed wrong control filename display in error message
  * recipients.c use users/recipients instead of control/recipients
  * renamed systemd unit file indimail.service to svscan.service
  * upgrade boot scripts if new package has updates
  * config-fast.sh - fixed syntax error due to unterminated variable
  * svctool --config=mysqldb indimail mysql db wasn't getting created
  * svctool --config=mysqldb fix user creation for oracle's brain dead mysql db
  * svctool,create_services,postinst - refactored code by improving calls to svctool
  * fixes for latest clamd/freshclam versions (ClamAV 0.99.2)
  * do systemctl daemon-reload if systemd unit file is changed
  * fixes in upgrade.sh, local_upgrade.sh scripts
  * qmail-greyd.c - fixed search record failure when linked list contained just 1 entry
  * changed logdir to /var/log/svc
  * removed programs copyemail, testmra, mail_report, execmysql
  * binary search implementation for USER_QUERY, PWD_QUERY, HOST_QUERY, ALIAS_QUERY
  * fixed syntax error with --qhpsi option changes in postinst file
  * qmail-greyd.c - added verbose variable to suppress few messages
  * qmail-greyd.c - initialize h_allocated, fix SIGUSR1 sig handler, expire records when loading context file
  * qmail-greyd.c - fixed record expiry logic
  * added post handle call in vaddaliasdomain

 -- Manvendra Bhangui <manvendra@indimail.org>  Sun, 04 Dec 2017 09:13:33 +0530
