#!/bin/sh
if [ ! -f ./configure -o ! -f Makefile -o ! -f Makefile.in ] ; then
autoreconf -fiv
fi
./configure --prefix=/usr --libexecdir=/usr/libexec/indimail --enable-plugindir=/usr/share/roundcubemail
