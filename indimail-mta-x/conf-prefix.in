PREFIX
This is the qmail parent directory for bin and sbin. It must be a local directory,
not shared among machines. This is where the bin and sbin subdir is present. All
binaries must be present in these two directories.
