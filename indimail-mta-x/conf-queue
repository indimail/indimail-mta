-DMULTI_QUEUE -DQUEUE_START=1 -DQUEUE_COUNT=10 -DQUEUE_MAX=20 -DQUEUE_LOAD=50

# MULTI_QUEUE makes indimail use multiple queues.
# QUEUE_COUNT is the upper limit on the number of queue that
# qmail-qread(8), qmail-multi(8), qmail-daemon(8), qmail-tcpok(8),
# qmail-tcpto(8) will process in the absence of QUEUE_COUNT
# environment variable.
